var assert = require('chai').assert,
    saveMakeup = require('../lib/action/handler/makeup.save');

describe('makeup.save.js', function() {
    it('save makeup', function(done) {
        done();
    });
});